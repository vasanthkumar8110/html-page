# mobile-legacy-ios8

Legacy client required to run on **real iOS 8 devices**.

Implementation requires a separate, older native/RN toolchain and build pipeline.

This app will integrate with the same backend to:
- login
- view shared content (minimum viable)
- request signed URLs for playback/download

