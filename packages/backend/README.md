# backend

Shared backend (single source of truth) for:
- Auth
- Workspace/file metadata
- Sharing/invites
- Signed URL generation for secure viewing/downloading

Video transcoding is optional and can be added via an async worker.

